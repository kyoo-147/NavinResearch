# Navin Research — Coming Soon

Static coming-soon page for `navinresearch.com`.

## Preview

```bash
python -m http.server 4173
```

The five source PNG files are preserved at the project root. Optimized WebP files in `assets/` are used in production.
