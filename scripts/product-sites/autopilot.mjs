import { escapeHtml, headMarkup } from "../product-renderer-helpers.mjs";
const e=escapeHtml;const normalizePath=(v="/")=>v==="/404.html"?v:(`/${String(v).replace(/^\/+|\/+$/g,"")}/`).replace("//","/");const ext=(h="")=>/^https?:|^mailto:/.test(h)?' target="_blank" rel="noopener noreferrer"':"";
const layoutFor=(p)=>p.path==="/404.html"?"error":/docs|github|software/.test(p.path)?"docs":/perception|localization|planning|control|simulation/.test(p.path)?"workflow":/research|benchmarks|releases/.test(p.path)?"timeline":/hardware|vehicle|platform/.test(p.path)?"specs":/privacy|terms/.test(p.path)?"ledger":/access/.test(p.path)?"availability":"editorial";
function nav(site,path){return site.navigation.map(item=>{const href=item.href||item.children?.[0]?.href||"/",active=normalizePath(href)===path||item.children?.some(c=>normalizePath(c.href)===path);if(item.children?.length)return `<details class="ap-nav-group"${active?" data-current":""}><summary>${e(item.label)}<span>+</span></summary><div>${item.children.map(c=>`<a href="${e(c.href)}"${normalizePath(c.href)===path?' aria-current="page"':""}${ext(c.href)}>${e(c.label)}</a>`).join("")}</div></details>`;return `<a href="${e(href)}"${active?' aria-current="page"':""}${ext(href)}>${e(item.label)}</a>`}).join("")}
function visual(page){if(page.visual?.src)return `<figure class="ap-media"><img src="${e(page.visual.src)}" alt="${e(page.visual.alt||page.visual.title)}" width="1400" height="900"><div><span>SOURCE / PROJECT-OWNED</span><span>CAPABILITY / NOT INFERRED</span></div><figcaption>${e(page.visual.caption||"")}</figcaption></figure>`;const items=(page.visual?.items||["Sensor","Model","Planner","Actuator"]).slice(0,6);return `<figure class="ap-telemetry"><div class="ap-telemetry__top"><span>RESEARCH LOOP / ILLUSTRATIVE</span><span>DRIVER / REQUIRED</span></div><div class="ap-road"><i></i><i></i><b>35<small>scale</small></b></div><ol>${items.map((item,index)=>`<li><span>${String(index+1).padStart(2,"0")}</span><b>${e(item)}</b><small>${index===items.length-1?"OUTPUT":"SIGNAL"}</small></li>`).join("")}</ol><figcaption>${e(page.visual?.caption||"Editorial system view; no physical capability is implied.")}</figcaption></figure>`}
function overview(page){return `<section class="ap-overview">${(page.sections||[]).map((s,i)=>`<article><span>${String(i+1).padStart(2,"0")}</span><div><h2>${e(s.title)}</h2><p>${e(s.body)}</p>${s.status?`<strong>${e(s.status)}</strong>`:""}</div>${s.points?.length?`<ul>${s.points.map(p=>`<li>${e(p)}</li>`).join("")}</ul>`:""}</article>`).join("")}</section>`}
function loop(page){return `<section class="ap-loop"><header><span>CLOSED LOOP</span><p>Signals are described separately from physical proof.</p></header><ol>${(page.sections||[]).map((s,i)=>`<li><div class="ap-loop__no">${String(i+1).padStart(2,"0")}</div><div><h2>${e(s.title)}</h2><p>${e(s.body)}</p>${s.status?`<mark>${e(s.status)}</mark>`:""}</div>${s.points?.length?`<ul>${s.points.map(p=>`<li>${e(p)}</li>`).join("")}</ul>`:""}</li>`).join("")}</ol></section>`}
function accessRegister(page){const tiers=[['READ','Source reading','Public references and repository history','No price announced'],['STUDY','Bounded study','Protocol design and evidence review','No price announced'],['PARTNER','Research conversation','Scope and governance discussion','No price announced']];return `<section class="ap-boundary"><div class="ap-access-register"><header><span>ACCESS REGISTER / STATUS ONLY</span><h2>Three ways to read the work.</h2><p>These are future program shapes, not offers. No price, deployment, fleet, customer, certification, or benchmark claim is made.</p></header><div class="ap-access-register__tiers">${tiers.map(([name,title,body,price])=>`<article><span>${e(name)}</span><h3>${e(title)}</h3><p>${e(body)}</p><strong>${e(price)}</strong><ul><li>Defined scope</li><li>Human review</li><li>No physical proof implied</li></ul></article>`).join('')}</div><div class="ap-access-register__compare"><h3>Comparison boundary</h3><table><thead><tr><th>Program</th><th>Includes</th><th>Price</th></tr></thead><tbody>${tiers.map(([name,title,body,price])=>`<tr><th scope="row">${e(name)}</th><td>${e(title)} · ${e(body)}</td><td>${e(price)}</td></tr>`).join('')}</tbody></table></div><div class="ap-access-register__faq"><h3>FAQ</h3><details><summary>Can I buy or deploy Autopilot?</summary><p>No. Autopilot is an open research reference and no commercial or deployment offer is announced.</p></details><details><summary>How do I discuss a program?</summary><p>Use the enterprise contact path to describe a research boundary; contact does not create access.</p></details></div></section>`}
function log(page){return `<section class="ap-log"><header><span>RESEARCH LOG</span><span>REPEATABLE / REVIEWABLE / BOUNDED</span></header>${(page.sections||[]).map((s,i)=>`<article><div><span>ENTRY ${String(i+1).padStart(3,"0")}</span><strong>${e(s.status||"OPEN QUESTION")}</strong></div><h2>${e(s.title)}</h2><p>${e(s.body)}</p>${s.points?.length?`<dl>${s.points.map((p,j)=>`<div><dt>R${j+1}</dt><dd>${e(p)}</dd></div>`).join("")}</dl>`:""}</article>`).join("")}</section>`}
function docs(page){return `<section class="ap-docs"><aside><span>TECHNICAL INDEX</span>${(page.sections||[]).map((s,i)=>`<a href="#a${i+1}">${String(i+1).padStart(2,"0")} ${e(s.title)}</a>`).join("")}</aside><article>${(page.sections||[]).map((s,i)=>`<section id="a${i+1}"><span>${String(i+1).padStart(2,"0")}</span><div><h2>${e(s.title)}</h2><p>${e(s.body)}</p>${s.status?`<mark>${e(s.status)}</mark>`:""}</div>${s.points?.length?`<pre><code>${s.points.map(p=>`> ${e(p)}`).join("\n")}</code></pre>`:""}</section>`).join("")}</article></section>`}
function availability(page){const sections=page.sections||[];return `<section class="ap-boundary"><div class="ap-boundary__track"><span>TEST BOUNDARY</span><div aria-hidden="true"><i></i><i></i></div><b>DRIVER<br>REQUIRED</b></div><div class="ap-boundary__register"><header><span>ACCESS REGISTER</span><strong>${e(sections[0]?.status||"NOT OFFERED")}</strong></header>${sections.map((section,index)=>`<article><span>${String(index+1).padStart(2,"0")}</span><h2>${e(section.title)}</h2><p>${e(section.body)}</p>${section.status?`<strong>${e(section.status)}</strong>`:""}</article>`).join("")}</div></section>`}

function errorPage(page){return `<section class="ap-error"><span>404 / TRACK NOT FOUND</span><h2>${e(page.headline||page.title||"This route is outside the track.")}</h2><p>${e(page.lede||page.description||"Return to the Autopilot index.")}</p><a href="/">Return to Autopilot ↗</a></section>`;}
function content(page,mode){if(mode==="error")return errorPage(page);if(page.path==="/access/")return accessRegister(page);if(mode==="ledger")return log(page);if(mode==="availability")return availability(page);if(mode==="workflow")return loop(page);if(mode==="timeline")return log(page);if(mode==="docs")return docs(page);return overview(page)}
function footer(product,site){return `<footer class="ap-footer"><div class="ap-footer__claim"><span>AUTOPILOT / RESEARCH</span><h2>Keep the driver in the sentence.</h2><p>${e(product.proofNote)}</p></div><nav aria-label="Footer">${site.footerGroups.map((g,i)=>`<section><span>${String(i+1).padStart(2,"0")}</span><h3>${e(g.title)}</h3>${g.links.map(l=>`<a href="${e(l.href)}"${ext(l.href)}>${e(l.label)}</a>`).join("")}</section>`).join("")}</nav><div class="ap-footer__base"><span>© <span data-current-year></span> Autopilot</span><a href="https://navinresearch.com/products/">Navin Research ↗</a></div></footer>`}
function render(product,site,page){const path=normalizePath(page.path),mode=layoutFor(page),home=path==="/",cta=page.cta||site.primaryCta;return `<!doctype html><html lang="en" class="no-js"><head>${headMarkup({product,page,path,layout:mode,isHome:home})}</head><body class="ap-body ap-${mode}" data-product="autopilot" data-route="${e(path)}"><a class="skip-link" href="#main-content">Skip to content</a><header class="ap-header"><a class="ap-logo" href="/" aria-label="Autopilot home"><i aria-hidden="true"></i>AUTOPILOT</a><div class="ap-status"><span>TRACK</span><b>RESEARCH / DRIVER SUPERVISED</b></div><button class="product-menu-button ap-menu-button" type="button" aria-controls="product-menu" aria-expanded="false" data-product-menu>MENU</button><div class="product-menu ap-menu" id="product-menu"><nav aria-label="Primary navigation">${nav(site,path)}</nav><a class="ap-access" href="/access/">ACCESS ↗</a></div></header><main id="main-content"><section class="ap-hero"><div class="ap-hero__copy"><p>${e(page.eyebrow||"AUTONOMOUS-DRIVING RESEARCH / SCALE MODEL")}</p><h1>${e(page.headline||page.title)}</h1><p>${e(page.lede||page.description)}</p>${home?`<div class="ap-actions"><a href="/platform/">EXPLORE THE LOOP</a><a href="/research/">READ THE RESEARCH LOG →</a></div>`:""}</div>${visual(page)}</section>${content(page,mode)}${cta?`<section class="ap-cta"><span>END OF SECTION / DRIVER STILL RESPONSIBLE</span><div><h2>${e(cta.title||"Continue without inflating the claim")}</h2><p>${e(cta.body||product.availability.body)}</p></div><a href="${e(cta.href)}"${ext(cta.href)}>${e(cta.label)} →</a></section>`:""}${home?`<section class="ap-proof"><header><span>PROOF REGISTER</span><h2>Source images are not capability claims.</h2></header><p>${e(product.proofNote)}</p><ul>${product.evidence.map(item=>`<li><span>${e(item.label)}</span><b>${e(item.value)}</b><small>${e(item.state)}</small></li>`).join("")}</ul></section>`:""}</main>${footer(product,site)}</body></html>`}
export default function renderAutopilot(product,site,page){return render(product,site,page).replace(/[ \t]+$/gm,"")};export{layoutFor,normalizePath};
